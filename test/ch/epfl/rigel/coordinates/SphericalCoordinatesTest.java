package ch.epfl.rigel.coordinates;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

/**
 * @author Bastien Faivre (310929)
 * @author Kamil Mellouk (312327)
 */

/**
 * make the class not abstract
 */

public class SphericalCoordinatesTest {

    /*@Test
    void returnLonTest() {
        assertEquals(1, new SphericalCoordinates(1,0).lon());
        assertEquals(2, new SphericalCoordinates(2,0).lon());
    }

    @Test
    void returnLonDegTest() {
        assertEquals(0, new SphericalCoordinates(0,1).lonDeg());
        assertEquals(90, new SphericalCoordinates(Math.PI/2,1).lonDeg());
    }

    @Test
    void returnLatTest() {
        assertEquals(1, new SphericalCoordinates(0,1).lat());
        assertEquals(2, new SphericalCoordinates(0,2).lat());
    }

    @Test
    void returnLatDegTest() {
        assertEquals(0, new SphericalCoordinates(1,0).latDeg());
        assertEquals(90, new SphericalCoordinates(1,Math.PI/2).latDeg());
    }

    @Test
    void throwExceptionWithEquals() {
        assertThrows(UnsupportedOperationException.class, () -> {
            new SphericalCoordinates(0,0).equals(new Object());
        });
    }

    @Test
    void throwExceptionWithHashCode() {
        assertThrows(UnsupportedOperationException.class, () -> {
            new SphericalCoordinates(0,0).hashCode();
        });
    }*/

}
